<%= form_for @developer do |f| %>

  <% if @developer.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@developer.errors.count, "error") %> prohibited
        this developer from being saved:
      </h2>
      <ul>
        <% @developer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :dv_name %><br>
    <%= f.text_field :dv_name %>
  </p>

  <p>
    <%= f.label :dv_description %><br>
    <%= f.text_area :dv_description %>
  </p>

  <p>
    <%= f.label :dv_devices %><br>
    <%= f.text_area :dv_devices %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
